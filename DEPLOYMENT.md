# 동해로 배포 안내

현재 버전은 `donghae-planner.html` 한 파일로 동작하는 정적 웹 애플리케이션입니다. 별도 백엔드와 비밀 키가 필요하지 않습니다.

## 배포 구성

- 시작 파일: `donghae-planner.html`
- 경로와 보안 헤더: `vercel.json`
- 날씨 예보: Open-Meteo 공개 예보 API
- 지도 연결: 네이버 지도 검색 링크
- 글꼴: Pretendard 1.3.9 가변 글꼴 CDN, 불러오기 실패 시 시스템 글꼴 사용

## 배포 절차

1. 이 폴더를 정적 호스팅 서비스의 프로젝트 루트로 지정합니다.
2. 별도 빌드 명령은 설정하지 않습니다.
3. 루트 경로가 `donghae-planner.html`로 연결되는지 확인합니다.
4. HTTPS 주소에서 일정 복사와 모바일 공유 기능을 확인합니다.
5. 운영 도메인에서 Open-Meteo 요청과 Pretendard 글꼴 파일이 정상적으로 허용되는지 확인합니다.

## 환경변수

필요한 환경변수는 없습니다. 브라우저나 배포 설정에 API 키를 추가하지 마십시오.

## 운영 전 확인

- 375px, 768px, 1024px, 1440px 화면의 레이아웃
- 장소 검색, 유형 필터, 추천 일정, 직접 선택 일정
- 네이버 지도 링크의 장소명과 중심 좌표
- 날씨 API 성공과 연결 실패 안내
- 키보드 포커스, 모바일 터치 영역, 화면 읽기 프로그램의 제목 구조
- 운영 주체의 이용약관, 개인정보 처리방침, 문의처 연결

엄격한 콘텐츠 보안 정책을 적용하는 경우 인라인 스타일과 스크립트를 별도 파일로 분리하고 해시 또는 nonce 정책을 적용하는 것을 권장합니다. 글꼴 파일을 자체 호스팅하면 외부 CDN 의존성을 제거할 수 있습니다.
