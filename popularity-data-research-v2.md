# 동해로 v2 인기 장소 데이터 조사

조사일: 2026-08-28  
조사 범위: 기존 50개 장소를 최근 방문량으로 추천할 수 있는 공식 공개 데이터와 상용 적용 조건

## 결론

임의의 관광지 50곳 모두에 대해 최근 실제 방문자 수를 같은 기준으로 반환하는 공개 OpenAPI는 확인되지 않았다.

공개 API 중 장소 단위 실제 방문에 가장 가까운 데이터는 한국문화관광연구원의 **관광자원통계서비스**이다. 그러나 조사 대상이 주요 유료관광지로 한정되며, 기존 50곳의 해변·항구·카페·식당·산책로 전체를 포괄하지 않는다. 따라서 v2에서는 다음 원칙을 권장한다.

1. 관광자원통계서비스에서 장소가 확인되는 경우에만 최신 공표 월의 `내국인방문객수 + 외국인방문객수`로 순위를 만든다.
2. 집계되지 않은 장소에 기존 `ap`, 카테고리 점수 또는 임의 가중치를 붙여 인기 장소처럼 추천하지 않는다.
3. 데이터가 있는 장소만 `공식 집계 인기 장소`로 보여 주고, 나머지 50곳은 검색 가능한 전체 장소로 유지한다.
4. 장소 선택 후 일정의 이동 순서는 기존 `leg` 정렬을 그대로 사용한다. 인기 데이터는 장소를 고르는 데만 사용하고 이동 순서를 바꾸지 않는다.
5. 전체 50곳의 동일 기준 방문량이 반드시 필요하면 한국관광 데이터랩 맞춤데이터 또는 이동통신 POI 데이터에 대한 별도 계약이 필요하다.

## 1. 장소 단위 실제 입장객 수: 우선 적용

### 한국문화관광연구원 관광자원통계서비스

- 공식 데이터: [한국문화관광연구원_관광자원통계서비스](https://www.data.go.kr/data/15000366/openapi.do)
- 의미: 전국의 **주요 유료관광지**에 대해 월별 내국인·외국인 방문객 수를 제공하는 행정 보고 통계이다.
- 호출 주소: `http://openapi.tour.go.kr/openapi/service/TourismResourceStatsService/getPchrgTrrsrtVisitorList`
- 응답 형식: XML
- 인증: 공공데이터포털에서 발급한 서비스키
- 장소 단위 공개 호출 여부: 가능하지만 이 통계가 조사하는 관광지에 한정된다. 임의 좌표나 임의 장소명을 입력해 방문량을 산출하는 API가 아니다.

요청 필터:

| 필드 | 필수 | 의미 |
|---|---:|---|
| `YM` | 예 | 조회 연월 `YYYYMM` |
| `SIDO` | 아니요 | 시도명 |
| `GUNGU` | 아니요 | 시군구명 |
| `RES_NM` | 아니요 | 관광지명 |

인증키와 페이지 처리는 공공데이터포털의 공통 요청 규칙을 따른다. 데이터셋 상세 명세에는 도메인 필터가 위와 같이 기재되어 있으며 응답에는 `numOfRows`, `pageNo`, `totalCount`가 포함된다.

주요 응답 필드:

| 필드 | 의미 |
|---|---|
| `ym` | 집계 연월 |
| `sido` | 시도 |
| `gungu` | 시군구 |
| `resNm` | 관광지명 |
| `csNatCnt` | 내국인 방문객 수 |
| `csForCnt` | 외국인 방문객 수 |
| `addrCd` | 지역 코드 |
| `rnum` | 결과 순번 |

공통 응답은 `resultCode`, `resultMsg`, `numOfRows`, `pageNo`, `totalCount`를 포함한다.

### 데이터 해석상 제한

한국관광 데이터랩은 이 자료를 기초지자체가 문화체육관광부·한국문화관광연구원에 보고하는 통계라고 설명한다. 또한 익년도 확정치 공표 전까지 수치는 잠정치이고, 지자체가 월별 값을 입력하지 않으면 `0`으로 표시될 수 있으며, 데이터랩 화면은 2주에 한 번 갱신된다고 안내한다. 따라서 `0`을 방문객 0명으로 단정하면 안 된다. [한국관광 데이터랩의 유료관광지 입장객 설명](https://datalab.visitkorea.or.kr/datalab/portal/loc/getAreaDataForm.do?callYn=Y&tabDiv=3)

관광지식정보시스템의 현행 메타데이터는 월별 조사, 분기 종료 45일 뒤 잠정치 공표, 매년 4월 말 또는 5월 초에 전년도 확정치 공표라고 명시한다. 개별 관광지 수요로만 사용해야 하고 지역 전체 관광객 총량으로 합산해서는 안 된다. [주요관광지점 입장객 통계 소개](https://know.tour.go.kr/stat/visitStatDis/introduce.do), [공표주기·자료 유의사항](https://know.tour.go.kr/stat/visitStatDis/table.do)

공공데이터포털은 이 API의 수정일을 2025-04-10으로 표시한다. 별도의 최신화 SLA는 명시하지 않는다. 구현에서는 현재 월을 곧바로 사용하지 말고, 결과가 존재하는 가장 최근 `ym`을 찾아 그 연월을 화면에 표시해야 한다.

### 기존 50곳 적용 가능성

`대왕암공원`, `성류굴`, `환선굴`처럼 집계 대상일 가능성이 있는 장소가 있으나, API 키 없이 실제 레코드와 최신 월을 확인할 수 없었다. 해변, 항구, 카페, 식당, 무료 산책로는 대체로 이 통계의 범위 밖일 가능성이 높다. 운영 전 다음 키로 명시적 매핑 테이블을 만들어야 한다.

`provider tuple = (sido, gungu, resNm)` → `PLACES.id`

자동 퍼지 매칭은 동명이인과 복합 장소명을 잘못 연결할 수 있으므로 운영 추천 점수에는 사용하지 않는 편이 안전하다. 별칭은 신원 확인용으로만 사용하며 점수 가중치로 사용하지 않는다.

## 2. 실제 방문과 혼동하면 안 되는 공식 데이터

### 한국관광공사 지역별 방문자 수

- 공식 데이터: [한국관광공사_빅데이터_지역별 방문자수_GW](https://www.data.go.kr/data/15101972/openapi.do)
- Base URL: `https://apis.data.go.kr/B551011/DataLabService`
- 광역 경로: `GET /metcoRegnVisitrDDList`
- 기초 경로: `GET /locgoRegnVisitrDDList`
- 인증: `serviceKey`
- 필수 입력: `MobileOS`, `MobileApp`, `startYmd`, `endYmd`; `pageNo`, `numOfRows`는 선택
- 주요 응답: `baseYmd`, `areaCode` 또는 `signguCode`, `areaNm` 또는 `signguNm`, `daywkDivCd`, `daywkDivNm`, `touDivCd`, `touDivNm`, `touNum`

이 데이터는 KT 내국인·SKT 외국인 이동통신 자료를 바탕으로 한 광역·기초지자체 방문자 추정치다. 한 사람이 2박 3일 머물면 일자별로 3명으로 집계되고, 관광 목적을 직접 확인한 값도 아니다. 무엇보다 **시군구 단위**이므로 같은 포항 안의 여러 장소 중 어느 장소가 더 많이 방문되었는지 판별할 수 없다. 50개 장소 순위에는 사용하지 말고 `최근 많이 찾은 지역` 같은 별도 지역 정보에만 사용해야 한다. 공식 v4.1 가이드의 갱신주기는 일 1회이지만, 이는 데이터 갱신 빈도이지 실제 실시간 유동인구를 뜻하지 않는다.

공식 조건은 무료, 이용허락범위 제한 없음, 개발계정 일 1,000건, 운영계정은 활용사례 등록 후 증설 신청 가능이다. 개발은 자동승인, 운영은 심의승인이다.

### 관광지 집중률 방문자 추이 예측

- 공식 데이터: [한국관광공사_관광지 집중률 방문자 추이 예측 정보](https://www.data.go.kr/data/15128555/openapi.do)
- Base URL: `https://apis.data.go.kr/B551011/TatsCnctrRateService`
- 경로: `GET /tatsCnctrRatedList`
- 의미: 2018년 이후 KT 이동통신 패턴을 학습해 조회일 기준 향후 30일의 혼잡도를 상대값으로 예측한다. 가장 붐비는 시기를 100으로 둔다.

정확한 요청 파라미터:

| 필드 | 필수 | 의미 |
|---|---:|---|
| `serviceKey` | 예 | 공공데이터포털 인증키 |
| `pageNo` | 예 | 페이지 번호 |
| `numOfRows` | 예 | 한 페이지 결과 수 |
| `MobileOS` | 예 | `IOS`, `AND`, `WEB`, `ETC` |
| `MobileApp` | 예 | 서비스명 |
| `areaCd` | 예 | 관광지 지역 코드 |
| `signguCd` | 예 | 관광지 시군구 코드 |
| `tAtsNm` | 아니요 | 관광지명 |
| `_type` | 아니요 | `json` 지정 가능, 기본 XML |

응답 필드는 `cnctrRate`, `baseYmd`, `areaCd`, `areaNm`, `signguCd`, `signguNm`, `tAtsNm`이다.

이 값은 실제 최근 방문객 수가 아니며 향후 집중률 예측이다. 실제 방문 추천의 폴백 점수로 섞지 말고, 사용한다면 `예상 방문 집중도`라는 별도 배지로만 보여야 한다. 공식 조건은 무료, 이용허락범위 제한 없음, 개발계정 일 1,000건이며 운영은 심의승인이다.

### 내비게이션 검색량

내비게이션 목적지 검색은 방문 의도를 나타내는 대리 지표일 뿐 실제 방문자가 아니다. 한국관광 데이터랩도 차량이동 기반 검색량은 실제 방문자 수와 차이가 있다고 명시한다. 또한 과거 `한국관광공사_관광빅데이터정보서비스` 데이터셋(공공데이터 ID `15081754`)은 현재 상세 페이지가 삭제되어 있었다. TMAP 제거 요구와 데이터 의미를 함께 고려하면 v2 장소 추천에 사용하지 않는 것이 타당하다.

### SK open API 장소 방문자 통계: 상용 대안이나 범위가 매우 제한적

SK텔레콤의 `장소 혼잡도` 상품은 지원 장소에 한해 장소별 최근 추정 방문자 수를 제공한다. KCTI 입장객 통계보다 시의성은 높지만, 공식 설명상 유동인구가 많은 쇼핑·여가 장소 약 200곳을 우선 제공하므로 기존 동해안 50곳을 포괄한다고 가정할 수 없다. 먼저 지원 장소 목록과 실제 교집합을 검사해야 한다. [SK open API 지원 장소 문서](https://openapi.sk.com/products/detail?linkMenuSeq=301)

- 인증: 요청 헤더 `appKey`
- 지원 장소: `GET https://apis.openapi.sk.com/puzzle/place/meta/pois`
- 목록 입력: `offset`, `limit` (`limit` 최대 1,000)
- 목록 응답 핵심: `poiId`, `poiName`
- 일별 방문자: `GET https://apis.openapi.sk.com/puzzle/place/visit/count/raw/daily/pois/{poiId}`
- 입력: `gender`, `ageGrp`, `startDate`, `endDate`, `date`, `range`
- 응답 핵심: `poiId`, `poiName`, `raw[].date`, `raw[].approxVisitorCount`

방문자 값은 해당 장소 주변에서 10분 이상 체류한 SKT 회선 중 거주·직장 인구를 제외한 뒤 통신사 점유율과 기지국 밀도 등을 보정한 **추정치**이다. 어제까지 최근 30일 범위를 제공하므로 실제 출입구 계수값과 동일하지 않다. [SK open API 일자별 장소 방문자 수 문서](https://openapi.sk.com/products/detail?linkMenuSeq=305)

공식 요금 페이지 기준 Free는 장소 목록·혼잡도·방문자 통계 합산 월 10건, Basic은 건당 33원과 일 100건, 해커톤 상품은 건당 11원과 월 3,000건 조건으로 표시된다. 상용 적용 전 최신 상품 가입 조건과 약관을 다시 확인해야 한다. [SK open API 요금 계산](https://openapi.sk.com/products/calc?menuSeq=263&svcSeq=56)

이 API는 TMAP API가 아니지만 SK 상용 데이터 의존성을 새로 추가한다. 사용자 요구가 외부 SK/TMAP 계열 의존성 제거까지 포함한다면 v2에는 넣지 않는 편이 일관적이다. 사용하더라도 `지원됨`이 확인된 장소만 `추정 방문자`로 표시하고 KCTI의 실제 입장객 수와 같은 순위표에 섞지 않아야 한다.

## 3. 전체 50곳을 동일 기준으로 다루려면

한국관광 데이터랩의 관광지별 대시보드는 데이터가 확보된 관광지만 표시한다고 안내한다. 즉 데이터랩 내부에도 모든 임의 관광지를 자동으로 포괄하는 것은 아니다. [관광지별 대시보드 안내](https://datalab.visitkorea.or.kr/datalab/portal/loc/getTourDataFormBeta.do)

한국관광산업포털의 맞춤데이터 제공은 이동통신 방문자 수와 내비게이션 검색량 등을 요청할 수 있지만, 데이터 제공사 계약에 따라 제공 범위가 제한되거나 절대값 대신 비율·추이만 제공될 수 있다고 명시한다. [한국관광산업포털 맞춤분석 안내](https://datalab.visitkorea.or.kr/common/board/Download.do?bcIdx=303330&cbIdx=1135&streFileNm=abc47cba-7e25-4577-b752-5037549eddb5.pdf)

따라서 전체 50곳을 같은 방식으로 평가하려면 다음 중 하나가 필요하다.

- 한국관광 데이터랩에 50개 POI의 제공 가능 여부와 상업 서비스 이용 범위를 문의한다.
- KT 등 이동통신 기반 POI 방문 분석 사업자와 별도 계약한다.
- 서비스 자체의 사용자 동의 기반 방문 체크인 데이터를 장기간 축적한다. 초기에는 데이터가 없으므로 즉시 대체재가 되지는 않는다.

## 4. v2 권장 구현 명세

### 추천 생성

1. 백엔드 배치가 최근 연월부터 거꾸로 조회하여 데이터가 존재하는 가장 최신 `YM`을 찾는다.
2. 울산광역시, 경상북도, 강원특별자치도 관련 레코드를 수집한다. 강원 지역 명칭 변경을 고려해 과거 `강원도` 값도 수용한다.
3. 검증된 `(sido, gungu, resNm)` 매핑으로 기존 `PLACES.id`에 연결한다.
4. `visitors = Number(csNatCnt) + Number(csForCnt)`로 계산한다.
5. 같은 `ym`의 `visitors` 내림차순으로만 인기 순위를 만든다. `ap`, 카테고리, 사진 유무, 이동거리 등은 인기 점수에 넣지 않는다.
6. API의 `0`은 미입력일 수 있으므로 추천 대상에서 제외하고 `집계 확인 필요`로 처리한다.
7. 사용자가 선택한 여행 범위 안에서 데이터가 있는 상위 장소를 제안한 뒤, 실제 일정은 `sortCorridor()`와 `leg` 값으로 다시 정렬한다.

응답 예시:

```json
{
  "status": "partial",
  "asOf": "202607",
  "source": "한국문화관광연구원 관광자원통계서비스",
  "coverage": { "matched": 8, "total": 50 },
  "places": [
    {
      "id": "example",
      "rank": 1,
      "visitors": 123456,
      "domesticVisitors": 120000,
      "foreignVisitors": 3456,
      "provisional": true
    }
  ]
}
```

`status`는 `available`, `partial`, `stale`, `unavailable` 중 하나로 제한하는 것이 좋다.

### API 장애·키 미설정 폴백

- 마지막 성공 캐시가 30일 이내이면 그대로 사용하고 `저장된 YYYY년 M월 집계`라고 표시한다.
- 캐시가 없거나 너무 오래되면 인기 자동추천을 중단하고 `공식 방문 집계를 불러올 수 없습니다. 전체 장소에서 직접 선택할 수 있습니다.`라고 표시한다.
- 기존 `ap` 기반 추천으로 조용히 되돌아가면 안 된다.
- 예측 집중률이나 지역 방문자 수를 장소별 실제 방문자 수처럼 합성하지 않는다.

### 호출량과 캐시

- 브라우저에서 50회 호출하지 않는다.
- 서버 배치 또는 서버리스 함수가 시도별·월별로 수집하고 결과를 24시간 이상 캐시한다.
- 타임아웃 5초, 네트워크 오류 또는 5xx에 한해 1회 지수 백오프 재시도, 동일 요청 동시 실행 방지(single flight)를 적용한다.
- 개발계정 일 1,000건 한도를 고려해 최근 월 탐색 결과도 캐시한다.

### 보안과 배포

- 환경변수: `DATA_GO_KR_SERVICE_KEY`
- 키는 HTML 또는 공개 JavaScript에 넣지 않는다.
- 브라우저는 자체 `/api/popularity`만 호출한다.
- `YM`, 지역, 장소 ID는 허용 목록으로 검증하고 사용자가 임의의 상류 URL을 지정하지 못하게 한다.
- 상류 응답 전체나 키가 포함된 URL을 운영 로그에 남기지 않는다.
- 공식 관광자원통계 문서는 HTTP URL만 기재한다. 2026-08-28 확인 시 HTTPS 호출은 응답 시간 초과, HTTP 호출은 키 없는 요청에 500을 반환했다. 운영 배포 전 제공기관에 HTTPS 지원과 오류 규약을 반드시 재확인해야 한다.

### 화면 표시 문구

- 권장: `2026년 7월 공식 입장객 집계 기준`
- 권장: `50곳 중 8곳에 공식 집계가 있습니다.`
- 권장: `잠정치이며 이후 조정될 수 있습니다.`
- 금지: `실시간 인기`, `현재 방문자`, `최근 사람이 가장 많은 곳` — 월별 보고 통계에는 부정확한 표현이다.
- 출처와 기준 연월은 추천 목록 가까이에 항상 표시한다.

## 5. 이용 조건과 상용 배포 확인

관광자원통계서비스는 무료, 이용허락범위 제한 없음, 개발·운영 자동승인으로 표시되며 개발계정 기본 트래픽은 일 1,000건이다. 운영계정은 활용사례 등록 후 증설 신청이 가능하다. 지역 방문자 수와 집중률 예측 API도 무료·이용허락범위 제한 없음이지만 운영단계는 심의승인이다.

공공데이터포털은 제3자 권리가 포함된 데이터는 별도 권리 확인이 필요하고, 공공누리 유형에 따라 출처표시·상업 이용·변형 조건이 달라질 수 있다고 안내한다. [공공데이터포털 이용정책](https://www.data.go.kr/ugs/selectPortalPolicyView.do)

각 데이터셋 페이지가 `이용허락범위 제한 없음`으로 표시되더라도 상용 배포 전 활용신청 승인 화면과 최신 이용조건을 다시 보관해야 한다. 제품에는 최소한 다음을 표시하는 편이 안전하다.

`데이터 출처: 한국문화관광연구원 관광자원통계서비스 / 기준: YYYY년 M월 / 조회: YYYY-MM-DD HH:mm`

## 최종 권고

v2의 즉시 구현은 **부분 커버리지의 공식 입장객 순위 + 전체 50곳 탐색** 구조가 적절하다. 실제 데이터가 없는 장소까지 점수화하는 순간 다시 규칙 기반 추천이 되므로, 데이터 부재를 명확하게 드러내는 것이 상품 신뢰도 측면에서 낫다. 전체 50곳을 동일 기준으로 추천하는 기능은 POI 이동통신 데이터 계약이 확보된 뒤 별도 버전에서 활성화해야 한다.
