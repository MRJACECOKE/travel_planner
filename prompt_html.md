# Claude Code 실행 명령 — 동해안 AI 드라이브·여행 추천 시스템

## 0. 최상위 실행 명령

당신은 이 프로젝트의 **Lead Full-Stack Engineer + Recommendation System Engineer + Travel Data Engineer + QA Engineer**다.

지금부터 아래 명세를 읽고 **질문하지 말고 즉시 프로젝트를 분석하고 구현하라.**

이 작업은 설계 상담이나 샘플 코드 작성이 아니다.

**실제로 실행 가능한 프로젝트를 직접 생성·수정·테스트하고, 발견한 오류를 스스로 수정하여 완료 상태까지 만들어야 한다.**

### 절대적인 작업 원칙

1. 사용자에게 중간 확인 질문을 하지 않는다.
2. 구현 여부를 다시 묻지 않는다.
3. 파일을 만들어도 되는지 묻지 않는다.
4. 라이브러리 선택을 묻지 않는다.
5. 디자인 선택을 묻지 않는다. 글꼴은 맑은 고딕로한다. 구어체, 문어체를 사용을 반드시 금지한다. 이상한 언어 및 자주 사용하지 않는 단어로 표현하지 말라. 한글 인코딩을 반드시 가능하게 하라. 
6. 사소한 요구사항 충돌은 스스로 합리적으로 판단한다.
7. 필요한 파일은 직접 생성한다.
8. 기존 파일이 있으면 먼저 분석한 뒤 필요한 부분을 수정한다.
9. 기존 기능을 불필요하게 삭제하지 않는다.
10. 오류가 발생하면 원인을 찾아 직접 수정한다.
11. 테스트 실패 상태로 작업을 종료하지 않는다.
12. API 키가 없어도 앱의 나머지 기능이 동작하도록 fallback을 만든다.
13. 외부 서비스 접근이 불가능하다고 전체 구현을 포기하지 않는다.
14. 실데이터를 확보할 수 없는 부분은 interface + fallback + demo data 구조로 완성한다.
15. 존재하지 않는 실제 후기나 최신 정보를 임의 생성하지 않는다.
16. 가능한 모든 구현과 검증을 현재 작업 세션 안에서 수행한다.

사용자에게 설명만 하지 말고 **파일을 직접 작성하라.**

---

# 1. 프로젝트 목표

다음 시스템을 구축한다.

> **2026년 8월 기준 최신 여행 데이터와 실제 여행자 후기를 검색 증강하여, 경상북도 동해안부터 강원도 고성까지의 여행지를 차량 이동 방향·주차·우회거리·운전 피로·기상상황·영업시간·시간 효율을 종합해 자동 일정화하고, 네이버 지도에서 한눈에 확인할 수 있도록 하는 PC/모바일 반응형 동해안 드라이브 여행 플래너.**

단순 관광지 목록 페이지가 아니다.

반드시 다음 네 가지 시스템이 결합되어야 한다.

```text
1. 최신 관광/후기 데이터 검색 증강
2. 차량 이동 동선 최적화
3. 다중 날씨 분석 및 우천 대응
4. 네이버 지도 기반 코스 시각화
```

---

# 2. 가장 중요한 추가 요구사항 — 우천 대응

## 매우 중요

**비가 예상되면 반드시 추천 로직과 일정 구성에 적극적으로 반영한다.**

비가 오는데 해변, 야외 전망대, 장시간 산책 코스를 그대로 추천하는 시스템은 실패한 시스템으로 간주한다.

단순히 화면에

```text
오늘 비
```

라고 표시하는 것으로 끝내지 않는다.

기상 상태가 **추천 점수, 장소 카테고리, 일정 순서, 이동 경로, 체류시간​**에 실제 영향을 주어야 한다.

---

# 3. 우천 시 추천의 최우선 전략

비가 예상되는 시간대에는 다음 우선순위를 적용한다.

```text
1순위: 차량 드라이브
2순위: 실내 볼거리 관광지
3순위: 실내/주차 편한 맛집
4순위: 전망 좋은 실내 카페
5순위: 시장·박물관·전시관·아쿠아리움 등 실내 관광
6순위: 비를 피해 짧게 볼 수 있는 야외 포인트
```

즉,

**우천 = 여행 취소**

가 아니라,

**우천 = 드라이브 + 실내 관광 중심 코스로 자동 전환**

하도록 구현한다.

---

# 4. 우천 전용 카테고리

기존 장소 카테고리에 반드시 다음 속성을 추가한다.

```javascript
weatherCategory: {
  indoor: true,
  outdoor: false,
  rainFriendly: true,
  heavyRainFriendly: true,
  windyWeatherFriendly: true
}
```

또는 이에 준하는 구조를 설계한다.

최소 카테고리는 다음과 같이 구분한다.

```text
DRIVE
INDOOR_ATTRACTION
OUTDOOR_ATTRACTION
FOOD
CAFE
MARKET
MUSEUM
EXHIBITION
AQUARIUM
OBSERVATORY
COAST
BEACH
WALK
PHOTO_SPOT
```

특히

```text
INDOOR_ATTRACTION
```

을 독립적인 핵심 추천 카테고리로 구현한다.

---

# 5. 실내 볼거리 정의

우천 시 다음과 같은 장소를 적극 발굴한다.

- 박물관
- 미술관
- 전시관
- 역사관
- 과학관
- 아쿠아리움
- 실내 전망시설
- 전통문화 전시시설
- 지역 역사 전시관
- 체험관
- 실내 관광시설
- 복합문화공간
- 대형 시장
- 아케이드형 전통시장
- 실내 또는 반실내 상업 관광시설
- 전망이 좋은 실내 카페
- 지역 특색을 체험할 수 있는 실내 장소

대한민국 구석구석 및 최신 후기 검색에서 이러한 장소를 적극적으로 탐색한다.

---

# 6. 우천 시 드라이브 우선

비가 오는 경우에도 운전 안전에 문제가 없는 수준이면 **드라이브 자체의 추천 비중을 높인다.**

예:

```text
비 오는 동해안
→ 장시간 해변 산책 제거
→ 해안도로 차량 이동
→ 바다 조망 가능한 포인트는 짧게 정차
→ 실내 관광
→ 식사
→ 전망 좋은 카페
→ 다음 도시로 이동
```

다만 다음 기상 조건에서는 해안도로 자체도 무조건 추천하지 않는다.

- 강풍
- 호우
- 태풍
- 도로 침수 위험
- 시야 불량
- 강한 돌풍
- 위험 수준의 강수량

이 경우:

```text
해안도로 감점
고속도로/주요 국도 우선
운전 거리 축소
실내 관광 확대
```

로 전환한다.

---

# 7. 우천 강도별 전략

다중 날씨 데이터를 바탕으로 최소 4단계로 분류한다.

```text
CLEAR
LIGHT_RAIN
RAIN
HEAVY_RAIN
```

권장 의미:

### CLEAR

강수 가능성이 낮다.

```text
야외 관광
해안 산책
전망대
해변
사진 포인트
드라이브
```

모두 정상 추천한다.

### LIGHT_RAIN

약한 비 또는 간헐적 비.

추천 우선순위:

```text
드라이브 ↑
실내 관광 ↑
카페 ↑
시장 ↑

긴 야외 산책 ↓
해변 체류 ↓
```

### RAIN

지속적인 비가 예상됨.

추천 우선순위:

```text
드라이브 매우 높음
실내 볼거리 매우 높음
실내 맛집 높음
카페 높음

야외 관광 낮음
해변 매우 낮음
산책 매우 낮음
```

### HEAVY_RAIN

강한 비 또는 위험한 수준.

추천 우선순위:

```text
주요 도로 이동
실내 관광
숙소 인근 일정
짧은 이동
주차 편한 장소
```

다음은 적극 감점한다.

```text
해안 산책
등산
노출된 전망대
방파제
해변
장시간 야외 체류
위험한 해안도로
```

---

# 8. 날씨 의사결정 함수

다음과 같은 함수 또는 이에 준하는 구조를 구현한다.

```javascript
classifyWeather()
calculateRainRisk()
calculateWeatherConfidence()
calculateWeatherFitScore()
applyRainStrategy()
applyHeavyRainSafetyPenalty()
rebuildScheduleForWeather()
```

예:

```javascript
function applyRainStrategy(place, weather) {
  // rain-friendly 여부와 시간대별 기상 상태를 분석하여
  // 추천 점수와 방문 우선순위를 조정
}
```

---

# 9. 기상 변화에 따른 일정 재생성

일정 생성 후에도 날씨 변화가 반영될 수 있도록 구조화한다.

예:

```text
원래 일정

09:00 해변
11:00 전망대
12:30 점심
14:00 산책
16:00 카페
```

오전 강수 가능성이 높아지면:

```text
09:00 해안 드라이브
10:00 박물관
12:00 점심
13:30 실내 관광
15:30 전망 카페
17:00 비가 약해질 경우 짧은 해안 포인트
```

로 변경한다.

---

# 10. 날씨보다 동선이 완전히 무너지면 안 됨

날씨에 대응한다고 도시를 앞뒤로 왕복하지 않는다.

금지:

```text
강릉
→ 양양
→ 비 때문에 다시 강릉
→ 다시 속초
```

대신:

```text
강릉 실내
→ 강릉 드라이브
→ 양양 실내
→ 속초
```

처럼 북상 방향을 유지한다.

우선순위:

```text
안전
>
불필요한 역주행 방지
>
동선 효율
>
날씨 최적화
>
개별 장소 인기
```

---

# 11. 최종 산출물

기본 프로젝트 구조:

```text
/index.html

/css/
  style.css
  responsive.css

/js/
  app.js
  config.js
  data.js
  recommendation.js
  routeOptimizer.js
  weather.js
  weatherStrategy.js
  map.js
  ui.js

/data/
  places.json
  reviews.json
  indoor-attractions.json

/README.md
```

필요하면 더 좋은 구조로 변경해도 된다.

단, 최종 사용자 인터페이스는 브라우저에서 사용할 수 있어야 한다.

---

# 12. 기술 선택

프레임워크가 반드시 필요한 상황이 아니라면 복잡도를 불필요하게 높이지 않는다.

기본적으로:

```text
HTML5
CSS3
Vanilla JavaScript
NAVER Maps JavaScript API
Fetch API
JSON
```

를 우선 고려한다.

기존 프로젝트가 React/Vue/Vite 등으로 이미 구성되어 있으면 기존 구조를 존중한다.

프로젝트를 무조건 새 프레임워크로 갈아엎지 않는다.

---

# 13. 디자인

## 금지

다크 모드를 구현하지 않는다.

```text
prefers-color-scheme: dark
dark theme
dark toggle
```

등을 추가하지 않는다.

라이트 모드 전용이다.

---

# 14. PC 레이아웃

권장:

```text
┌───────────────────────────────────────────────────────────────┐
│ 동해안 드라이브 플래너                                      │
│ 최신 후기 + 날씨 + 차량동선                                │
├───────────────┬──────────────────────────┬────────────────────┤
│ 여행조건       │ 네이버 지도               │ DAY 일정           │
│               │                           │                    │
│ 출발           │                           │                    │
│ 목적지         │                           │                    │
│ 기간           │                           │                    │
│ 스타일         │                           │                    │
├───────────────┴──────────────────────────┴────────────────────┤
│ 오늘/내일 날씨 │ 우천 전략 │ 추천 관광지 │ 실내 볼거리        │
└───────────────────────────────────────────────────────────────┘
```

---

# 15. 모바일 레이아웃

```text
여행 조건
↓
코스 만들기
↓
여행 요약
↓
우천/날씨 알림
↓
네이버 지도
↓
DAY 일정
↓
실내 볼거리
↓
맛집
↓
카페
↓
지역별 날씨
```

360px 정도의 모바일 화면에서도 가로 스크롤 없이 정상 작동해야 한다.

---

# 16. 날씨 경고 UI

비 예보가 있으면 코스 생성 직후 눈에 잘 띄게 표시한다.

예:

```text
☔ DAY 2 강릉·양양 우천 가능성 높음

오전 강수 가능성이 높아
해변·산책 일정을 줄이고

해안 드라이브 + 실내 관광 + 전망 카페
중심으로 일정을 조정했습니다.
```

또는:

```text
⚠ 강풍 동반 비 예보

해안도로 일부 구간을 낮게 평가하고
주요 도로 이동 중심으로 재구성했습니다.
```

---

# 17. 사용자용 날씨 대응 필터

다음 옵션을 제공한다.

```text
[자동 날씨 최적화 ON]
```

기본값:

```text
ON
```

또한 다음 빠른 재구성 버튼을 제공한다.

```text
[비 오는 날 코스로 변경]
[드라이브 늘리기]
[실내 볼거리 늘리기]
[야외 일정 줄이기]
```

---

# 18. 여행 범위

관광 추천 기본 범위:

```text
경주
→ 포항
→ 영덕
→ 울진
→ 삼척
→ 동해
→ 강릉
→ 양양
→ 속초
→ 고성
```

부산과 울산은 관광 추천지역에서 제외한다.

단,

```text
부산 출발
울산 출발
```

은 허용한다.

---

# 19. 사용자 입력

필수:

- 출발지
- 여행 시작지역
- 최종 여행지역
- 출발 날짜
- 여행 일수
- 출발시간
- 마지막 날 종료 희망시간

선택:

- 드라이브 선호도
- 여행 강도
- 음식 관심도
- 카페 관심도
- 사진 관심도
- 자연 관심도
- 실내 관광 관심도
- 시장 관심도
- 해안도로 선호도
- 하루 최대 운전시간
- 하루 최대 방문 장소
- 동행 형태

---

# 20. 반드시 사용하는 관광 데이터 — 대한민국 구석구석

대한민국 구석구석을 반드시 주요 데이터 소스로 사용한다.

```text
https://korean.visitkorea.or.kr/
```

가능하면 한국관광공사 TourAPI도 연동 가능한 구조로 만든다.

다음 정보를 기본 관광 데이터로 활용한다.

```text
명칭
주소
좌표
관광 유형
소개
영업/운영시간
휴무일
주차
연락처
홈페이지
```

특히 우천 대응을 위해 관광지 유형을 분석하여

```text
실내
실외
부분 실내
우천 적합
우천 부적합
```

태그를 추가한다.

---

# 21. 검색 증강 시스템

2026년 8월 시점의 가능한 최신 공개 정보를 활용할 수 있는 구조로 만든다.

후보 정보원:

- 대한민국 구석구석
- 네이버 검색
- 네이버 블로그 검색 결과
- 다음 검색
- 다음 블로그
- 여행 블로그
- 공개 SNS
- 여행 커뮤니티
- 맛집 후기
- 카페 후기
- 드라이버 후기
- 지역 관광 관련 콘텐츠

법적/기술적 제한을 우회하는 무단 크롤링은 하지 않는다.

구조:

```text
검색
↓
출처 저장
↓
내용 정제
↓
광고성 감점
↓
중복 제거
↓
후기 요약
↓
장소별 특징 생성
↓
추천 데이터
```

---

# 22. 절대 가짜 데이터를 만들지 말 것

실제 검색을 수행하지 못한 상태라면

```text
2026년 최근 후기에서 인기
```

등의 문장을 임의로 생성하지 않는다.

대신:

```text
최근 후기 데이터 연결 필요
```

또는:

```text
DEMO DATA
```

라고 명확하게 표시한다.

---

# 23. 후기 신뢰도

광고성 감점 키워드 예:

```text
광고
협찬
체험단
원고료
제공받아
소정의
업체로부터
지원받아
```

이러한 표현만으로 무조건 제거하지는 말고 commercialPenalty를 높인다.

신뢰도 가점 요소:

- 주차 경험
- 실제 대기시간
- 도로 접근성
- 방문시간
- 이동동선
- 실제 단점
- 혼잡도
- 재방문 여부
- 구체적인 음식 평가
- 독립적인 여러 후기가 같은 특징을 언급

---

# 24. 추천 엔진

단순 룰 기반 추천 금지.

각 장소에 종합 점수를 계산한다.

예:

```javascript
score =
  recentScore * 0.15 +
  reviewQualityScore * 0.14 +
  routeFitScore * 0.17 +
  driveValueScore * 0.11 +
  parkingScore * 0.08 +
  scenicScore * 0.08 +
  timeEfficiencyScore * 0.09 +
  weatherFitScore * 0.12 +
  userPreferenceScore * 0.06
  -
  commercialPenalty
  -
  detourPenalty
  -
  backtrackingPenalty
  -
  weatherRiskPenalty;
```

정확한 가중치는 개선 가능하다.

단,

```text
routeFitScore
weatherFitScore
backtrackingPenalty
weatherRiskPenalty
```

는 반드시 존재해야 한다.

---

# 25. 우천 추천 점수

비가 예상되는 경우:

### 실내 관광

```text
weatherFitScore 크게 가점
```

### 드라이브 포인트

운전 안전에 문제가 없으면:

```text
driveValueScore 가점
weatherFitScore 가점
```

### 해변

```text
weatherFitScore 감점
```

### 장거리 산책

```text
weatherRiskPenalty 증가
```

### 야외 전망대

비와 풍속에 따라 감점한다.

### 주차가 어려운 장소

비 오는 날에는 평소보다 추가 감점한다.

---

# 26. 차량 동선 최적화

자가용 기준이다.

반드시 고려:

```text
실제 차량 이동시간
우회시간
도로 방향
고속도로
국도
해안도로
주차
진입 난이도
체류시간
영업시간
다음 장소 방향
하루 총 운전시간
운전 피로
날씨
```

---

# 27. 지그재그 절대 방지

북상 여행 예:

좋음:

```text
포항 남부
→ 포항 북부
→ 영덕 남부
→ 영덕 북부
→ 울진
→ 삼척
```

나쁨:

```text
포항
→ 영덕
→ 포항 방향
→ 울진
```

다음 함수를 구현한다.

```javascript
calculateBacktrackingPenalty()
detectRouteDirection()
calculateDirectionalProgress()
```

---

# 28. 도로 선택 로직

사용자가 요구한 차량 이동 특성을 반영한다.

예:

### 울산 → 포항

빠른 장거리 이동이 필요하면 고속도로 우선.

### 포항 → 영덕

고속도로/국도/해안도로를 목적에 맞게 조합.

### 경관 구간

해안도로를 사용할 가치가 높은 구간은 scenicDrive로 표시.

### 우천

약한 비:

```text
안전한 해안 드라이브 가능
```

강한 비/강풍:

```text
주요 도로 우선
```

---

# 29. 네이버 지도

NAVER Maps JavaScript API v3 기반.

필수:

- 장소 마커
- 방문 번호
- DAY 구분
- 관광지
- 실내 관광지
- 맛집
- 카페
- 숙박
- 드라이브 포인트
- 지도 범위 자동 조절
- 장소 클릭 정보
- DAY 필터
- 전체 일정
- 일정 카드 번호와 지도 마커 번호 일치

가능하다면 경로를 시각적으로 연결한다.

---

# 30. 우천 장소 지도 표시

실내 장소를 지도에서 쉽게 구분한다.

예:

```text
☂ 실내
🚗 드라이브
🍽 맛집
☕ 카페
🌊 야외
```

아이콘이나 명확한 텍스트 배지를 사용한다.

색상만으로 의미를 전달하지 않는다.

---

# 31. 외부 지도 API 실패

NAVER API 키가 없거나 지도 호출이 실패해도:

```text
여행 조건
추천 코스
DAY 일정
날씨
장소 카드
```

는 정상 작동해야 한다.

---

# 32. 다중 날씨 엔진

하나의 날씨 공급자만 사용하지 않는다.

가능한 소스:

```text
기상청
OpenWeather
Open-Meteo / ECMWF 계열
```

각 지역:

```text
경주
포항
영덕
울진
삼척
동해
강릉
양양
속초
고성
```

실제 여행 범위만 조회한다.

---

# 33. 오늘/내일 날씨

최소:

```text
날씨
최저기온
최고기온
강수확률
예상 강수량
풍속
```

가능하면:

```text
돌풍
습도
체감온도
일출
일몰
```

도 포함한다.

---

# 34. 다중 날씨 종합

예:

```text
강릉

기상청
강수 70%

OpenWeather
강수 55%

ECMWF
강수 62%

종합
62%

예보 신뢰도
높음
```

공급자 차이가 큰 경우:

```text
예보 불확실성 높음
```

표시.

---

# 35. 날씨 신뢰도

다음 함수를 만든다.

```javascript
calculateForecastAgreement()
```

예측 공급자들의 편차가 작으면 confidence 상승.

편차가 크면 confidence 하락.

일정 추천 설명에도 반영한다.

예:

```text
3개 예보원이 오전 강수를 공통적으로 예측하여
실내 관광 중심으로 일정을 변경했습니다.
```

---

# 36. 장소 데이터 구조

예:

```javascript
{
  id: "gangneung-001",
  name: "",
  region: "강릉",

  category: "INDOOR_ATTRACTION",
  subCategory: "museum",

  lat: 0,
  lng: 0,

  indoor: true,
  rainFriendly: true,
  heavyRainFriendly: true,

  parking: {
    available: true,
    difficulty: "easy",
    covered: false,
    description: ""
  },

  openingHours: "",
  closedDays: "",

  recommendedStayMinutes: 60,

  weatherProfile: {
    clear: 80,
    lightRain: 95,
    rain: 100,
    heavyRain: 90,
    strongWind: 90
  },

  scores: {
    recent: 0,
    reviews: 0,
    scenic: 0,
    parking: 0,
    drive: 0,
    routeFit: 0,
    weatherFit: 0,
    indoorValue: 0
  },

  reviewSummary: "",
  positives: [],
  negatives: [],

  sources: []
}
```

---

# 37. 우천 설명 가능성

사용자에게 다음처럼 이유를 보여준다.

```text
☔ 우천 대체 추천

오전 강수 가능성이 높아
해변 산책 대신 이 장소를 추천했습니다.

실내 관람 가능
주차 가능
현재 북상 동선 추가 우회 6분
예상 체류 70분
```

---

# 38. 일정 예시 — 맑음

```text
DAY 2

08:30 출발
09:10 해변
10:30 해안 전망대
12:00 점심
13:30 해안도로
15:00 관광지
17:00 카페
```

---

# 39. 일정 예시 — 우천

동일 지역이라도 비가 예상되면:

```text
DAY 2 ☔ 우천 대응

08:30 출발

09:00
해안 드라이브
차량 체류 중심

10:20
실내 박물관
체류 70분

12:00
주차 편한 지역 맛집

13:30
실내 관광시설

15:00
바다 전망 카페

16:30
강수 약화 예상 시
짧은 전망 포인트

17:00
다음 도시 이동
```

이러한 차이가 실제 추천 엔진에서 발생해야 한다.

정적인 예시만 넣고 끝내면 안 된다.

---

# 40. 식사 배치

점심과 저녁은 시간대에 맞게 배치한다.

주차 가능성과 현재 이동방향을 중요하게 평가한다.

비 오는 날은:

```text
주차 편의
주차장에서 매장까지 이동거리
실내 대기 가능 여부
```

도 가능하면 고려한다.

---

# 41. 카페 배치

카페는 단순 인기순 금지.

평가:

```text
현재 이동 방향
주차
전망
최근 후기
혼잡
좌석
실내성
우천 적합성
```

비가 오면:

```text
바다 조망 실내 카페
큰 창
주차 편한 카페
```

의 가치가 상승할 수 있다.

실제 근거가 있을 때만 반영한다.

---

# 42. 추천 결과

코스 생성 후 요약:

```text
울산 → 강릉
3박 4일

총 예상 주행
약 610km

예상 운전
약 11시간

관광지
8곳

실내 볼거리
4곳

드라이브 구간
6개

맛집
5곳

카페
4곳

☔ 우천 대응 일정
DAY 2
```

---

# 43. DAY 카드

각 DAY에 다음 정보:

```text
DAY 번호
지역
날씨
우천 여부
예상 주행거리
예상 운전시간
방문 장소
식사
카페
드라이브 구간
숙박지역
```

---

# 44. 장소 카드

반드시:

```text
장소명
지역
카테고리
실내/실외
우천 적합도
추천점수
추천 이유
최근 후기 요약
장점
주의
주차
운영시간
체류시간
추가 우회시간
다음 목적지 이동시간
출처
```

---

# 45. 제외 이유

예:

```text
이번 일정에서 제외

현재 북상 동선에서
약 32분의 역주행 발생
```

또는:

```text
우천 시 야외 체류 비중이 높아
대체 실내 관광지를 우선 추천
```

또는:

```text
강풍 예상으로 해안 전망 포인트 제외
```

---

# 46. 데이터 최신성

표시:

```text
관광 기본정보 확인일
최근 후기 기준일
날씨 갱신시간
```

허위 날짜 금지.

---

# 47. 앱 데이터 상태 표시

실데이터:

```text
LIVE DATA
```

샘플:

```text
DEMO DATA
```

혼합:

```text
PARTIAL LIVE DATA
```

등을 명확히 구분할 수 있다.

---

# 48. 일정 생성 파이프라인

사용자가 `동해안 코스 만들기`를 누르면:

```text
1. 입력 검증
2. 여행 지역 범위 계산
3. 부산/울산 관광 후보 제거
4. 지역별 후보 로드
5. 대한민국 구석구석 데이터 병합
6. 최신 후기 데이터 병합
7. 광고성 후기 감점
8. 지역별 날씨 로드
9. 다중 날씨 종합
10. 시간대별 우천 위험 계산
11. 장소별 날씨 적합도 계산
12. 실내 관광 후보 강화
13. 드라이브 후보 평가
14. 강풍/호우 위험 도로 감점
15. 사용자 선호 계산
16. 역주행 감점
17. 우회거리 감점
18. 영업시간 검사
19. 식사시간 배치
20. 카페 배치
21. DAY별 목적지역 결정
22. 차량 이동순서 최적화
23. 우천 대응 일정 재배치
24. 하루 운전시간 검사
25. DAY 일정 생성
26. 추천/제외 이유 생성
27. 지도 갱신
28. UI 렌더링
```

---

# 49. 핵심 함수

최소 다음 책임을 분리한다.

```javascript
getCandidatePlaces()
filterPlacesByTravelRange()
mergeVisitKoreaData()
mergeReviewData()

calculateReviewQuality()
calculateCommercialPenalty()
calculatePlaceScore()

detectTravelDirection()
calculateBacktrackingPenalty()
calculateDetourPenalty()

fetchWeatherProviders()
normalizeWeatherData()
aggregateWeather()
calculateForecastAgreement()
classifyWeather()
calculateRainRisk()
calculateWeatherFit()

getIndoorCandidates()
getDriveCandidates()
applyRainStrategy()
applyHeavyRainSafetyPenalty()

optimizeDailyRoute()
buildDailySchedule()
rebuildScheduleForWeather()

renderMap()
renderSchedule()
renderWeather()
renderRainNotice()
```

---

# 50. 데모 데이터

실 API가 없어도 시스템의 전체 기능을 테스트할 수 있게 한다.

10개 지역:

```text
경주
포항
영덕
울진
삼척
동해
강릉
양양
속초
고성
```

각 지역 최소:

```text
관광지
실내 볼거리
드라이브 포인트
맛집
카페
```

샘플 데이터에는 반드시 DEMO 표시.

---

# 51. 우천 데모 데이터도 반드시 포함

맑음 데이터만 만들지 않는다.

테스트용으로 최소:

```text
맑음
약한 비
비
강한 비
```

4가지 기상 시나리오가 있어야 한다.

개발자 모드 또는 테스트 함수로 날씨를 강제할 수 있으면 더 좋다.

예:

```javascript
?weather=rain
?weather=heavy-rain
```

또는 테스트 fixture 사용.

---

# 52. 필수 테스트 A — 기본 북상

```text
출발
울산

여행 시작
포항

종료
강릉

3박 4일
```

검증:

```text
포항
→ 영덕
→ 울진
→ 삼척
→ 동해
→ 강릉
```

기본 방향 유지.

---

# 53. 필수 테스트 B — 우천

조건:

```text
포항 → 강릉
3박 4일

DAY 2
울진/삼척 비
```

반드시 검증:

- 야외 장시간 관광 감소
- 실내 관광 증가
- 드라이브 비중 증가
- 해변 산책 감소
- 북상 방향 유지
- 강우 대응 이유 표시
- 실내 관광지 카드 생성
- 우천 경고 UI 표시

---

# 54. 필수 테스트 C — 폭우/강풍

조건:

```text
삼척
강한 비
강풍
```

검증:

- 위험한 해안 포인트 감점
- 장시간 해안 산책 제거
- 실내 관광 우선
- 주요도로 이동 우선
- 안전 경고 표시

---

# 55. 필수 테스트 D — 예보 불일치

예:

```text
기상청 20%
OpenWeather 75%
ECMWF 50%
```

검증:

```text
예보 불확실성 높음
```

표시.

추천은 한 공급자의 값만 믿지 않는다.

---

# 56. 필수 테스트 E — 서울 출발

```text
서울
→ 강릉
→ 고성
2박 3일
```

검증:

```text
강릉
→ 양양
→ 속초
→ 고성
```

경북 추천 없음.

---

# 57. 필수 테스트 F — 범위 제한

```text
대구
→ 경주
→ 속초
4박 5일
```

고성 추천 없음.

---

# 58. 브라우저 QA

최종적으로 가능하면 실제 브라우저 또는 프로젝트에서 사용할 수 있는 테스트 도구로 검증한다.

검증:

- HTML 렌더링
- console error
- network 실패 대응
- 버튼 작동
- form 작동
- 일정 생성
- DAY 변경
- 지도 fallback
- 날씨 fallback
- 모바일
- PC

---

# 59. 모바일 QA

최소 다음 width 기준을 확인한다.

```text
360
390
430
768
```

검증:

- 가로 스크롤 없음
- 버튼 터치 가능
- 지도 사용 가능
- 긴 장소명 깨짐 없음
- DAY 카드 읽기 가능
- 날씨 카드 읽기 가능

---

# 60. PC QA

최소:

```text
1280
1440
1920
```

에서 레이아웃이 지나치게 늘어나지 않도록 한다.

---

# 61. API 실패 테스트

다음 각각 실패시키고 확인한다.

```text
NAVER Maps 실패
기상청 실패
OpenWeather 실패
Open-Meteo 실패
TourAPI 실패
review API 실패
```

하나가 실패해도 전체 앱이 멈추면 안 된다.

---

# 62. 외부 API 키

하드코딩 금지.

예:

```javascript
export const CONFIG = {
  NAVER_MAP_CLIENT_ID: '',
  TOUR_API_KEY: '',
  KMA_API_KEY: '',
  OPENWEATHER_API_KEY: ''
};
```

클라이언트 노출이 적절하지 않은 키는 실제 운영환경에서 서버 프록시가 필요하다는 점을 README에 명시한다.

---

# 63. README 필수 내용

```text
프로젝트 목적
실행 방법
디렉터리 구조
추천 알고리즘
차량 동선 알고리즘
우천 대응 알고리즘
실내 관광 분류 방식
다중 날씨 종합 방식
NAVER 지도 설정
TourAPI 설정
기상청 설정
OpenWeather 설정
Open-Meteo 설정
검색 증강 구조
데모 데이터 설명
실데이터 연결 방법
API 오류 fallback
배포 방법
보안 주의사항
```

---

# 64. 작업 실행 순서

이 순서로 실제 작업한다.

### STEP 1

현재 디렉터리 전체를 확인한다.

### STEP 2

기존 프로젝트 및 의존성을 분석한다.

### STEP 3

구현 계획을 내부적으로 정리한다.

**계획 승인을 사용자에게 요청하지 않는다.**

### STEP 4

필요한 파일을 생성/수정한다.

### STEP 5

기본 UI를 완성한다.

### STEP 6

여행 조건 입력을 구현한다.

### STEP 7

데이터 모델을 구현한다.

### STEP 8

차량 추천 점수를 구현한다.

### STEP 9

동선 최적화를 구현한다.

### STEP 10

날씨 통합 모듈을 구현한다.

### STEP 11

**우천 전략 엔진을 반드시 구현한다.**

### STEP 12

**실내 관광 카테고리를 반드시 구현한다.**

### STEP 13

네이버 지도 모듈을 구현한다.

### STEP 14

추천 결과 UI를 구현한다.

### STEP 15

우천/폭우 테스트를 수행한다.

### STEP 16

모바일/PC QA를 수행한다.

### STEP 17

오류를 수정한다.

### STEP 18

README를 완성한다.

### STEP 19

전체 프로젝트를 다시 검사한다.

---

# 65. 중간 질문 금지 규칙

구현 중 다음과 같은 상황에서도 질문하지 않는다.

```text
어떤 디자인을 원하시나요?
API 키가 있나요?
React로 할까요?
Vanilla JS로 할까요?
샘플 데이터를 넣을까요?
파일을 만들어도 될까요?
```

묻지 않는다.

적절한 기본값을 사용하고 구현한다.

API 키가 없으면 placeholder + fallback.

데이터가 없으면 DEMO DATA.

---

# 66. 작업이 막혔을 때

외부 API, 인증, 인터넷 접근 또는 서비스 제한으로 일부 기능을 실제 호출할 수 없다면:

1. 해당 기능을 포기하지 않는다.
2. adapter/interface를 작성한다.
3. mock/fallback을 작성한다.
4. 실제 키를 입력하면 교체 가능하도록 만든다.
5. README에 연결 방법을 작성한다.

---

# 67. 완료 기준

다음이 모두 만족되어야 완료다.

```text
[ ] 실행 가능한 UI
[ ] 라이트 모드
[ ] PC 반응형
[ ] 모바일 반응형
[ ] 부산/울산 관광 추천 제외
[ ] 여행지역 범위 조절
[ ] 차량 이동 중심
[ ] 북상/남하 방향 유지
[ ] 역주행 감점
[ ] 우회거리 감점
[ ] 주차 평가
[ ] 대한민국 구석구석 데이터 구조
[ ] 최신 후기 검색 증강 구조
[ ] 광고성 후기 감점
[ ] 네이버 지도
[ ] 지도 fallback
[ ] 오늘/내일 날씨
[ ] 복수 날씨 공급자
[ ] 예보 일치도
[ ] 우천 위험 계산
[ ] 우천 시 드라이브 우선
[ ] 우천 시 실내 볼거리 증가
[ ] 실내 관광 독립 카테고리
[ ] 폭우/강풍 안전 감점
[ ] 우천 일정 자동 재구성
[ ] 추천 이유
[ ] 제외 이유
[ ] DAY 일정
[ ] 맛집
[ ] 카페
[ ] 영업시간
[ ] 체류시간
[ ] 이동시간
[ ] README
[ ] JS 치명적 오류 없음
[ ] API 실패 시 앱 중단 없음
```

하나라도 구현 가능한 항목이 빠져 있다면 작업을 완료했다고 선언하지 않는다.

---

# 68. 실패로 간주되는 구현

아래 중 하나라도 해당하면 미완성이다.

### 실패 1

비 예보를 화면에 표시만 하고 추천에 반영하지 않음.

### 실패 2

비가 오는데 야외 장소가 그대로 최상위 추천됨.

### 실패 3

`실내 관광`이 독립 카테고리로 존재하지 않음.

### 실패 4

우천 시 드라이브 가중치 조절이 없음.

### 실패 5

폭우/강풍에도 해안도로를 무조건 추천.

### 실패 6

날씨 공급자 하나만 사용.

### 실패 7

날씨 때문에 코스가 지그재그로 변함.

### 실패 8

지도 API 키가 없어서 전체 앱이 멈춤.

### 실패 9

2026년 최신 데이터인 것처럼 가짜 후기를 생성.

### 실패 10

코드를 만들지 않고 설명만 제공.

---

# 69. 구현 품질 기준

사용자는 실제 여행 직전에 이 시스템을 사용할 수 있어야 한다.

따라서 추천 화면에서 최소한 다음 질문에 답할 수 있어야 한다.

```text
오늘 어디까지 갈 것인가?
어떤 순서로 갈 것인가?
왜 이 장소를 추천했는가?
주차 가능한가?
몇 분 머물 것인가?
다음 장소까지 얼마나 걸리는가?
지금 방향에서 우회가 심한가?
비가 오면 어디를 가야 하는가?
비가 와도 드라이브할 만한 구간은 어디인가?
실내에서 볼 만한 곳은 어디인가?
폭우나 강풍이면 무엇을 피해야 하는가?
오늘과 내일 지역별 날씨는 어떤가?
```

---

# 70. 최종 핵심 명령

이 프로젝트에서 **우천 대응은 부가기능이 아니다.**

다음은 시스템의 핵심 추천 원칙이다.

> **날씨가 좋으면 해안 드라이브 + 야외 관광 + 맛집 + 카페를 균형 있게 추천하고, 비가 예상되면 차량 이동 방향을 유지하면서 드라이브 비중과 실내 볼거리 관광 비중을 적극적으로 높여 일정을 자동 재구성한다. 호우·강풍 등 운전 위험이 커지면 해안 노출 구간을 감점하고 주요 도로 + 실내 관광 중심으로 안전하게 전환한다.**

그리고 다시 강조한다.

> **비가 예상되는 것을 반드시 추천 엔진에 반영한다.**
>
> **우천 시 드라이브를 우선적으로 활용한다.**
>
> **우천 시 실내 볼거리 관광지를 반드시 독립 카테고리로 적극 추천한다.**
>
> **날씨를 단순 표시만 하는 구현은 허용하지 않는다.**

---

# 71. 지금 즉시 실행

지금부터:

```text
프로젝트 검사
→ 파일 생성/수정
→ 기능 구현
→ 테스트
→ 오류 수정
→ 모바일/PC 검증
→ README 작성
→ 최종 검증
```

순서로 실제 작업을 수행하라.

사용자에게 추가 정보를 요청하지 말고 현재 명세에서 합리적인 기본값을 결정하여 진행하라.

최종 응답에서는 장황한 설명 대신 다음을 보고한다.

```text
1. 생성/수정한 주요 파일
2. 구현한 핵심 기능
3. 수행한 테스트
4. 테스트 결과
5. API 키가 필요한 항목
6. 남아 있는 실제 외부 데이터 연결 사항
```

**설명용 목업에서 끝내지 말고 실행 가능한 프로젝트를 완성하라.**