# 동해로 작업 인계

## 현재 제품

- 제품명: 동해로
- 대상: 울산에서 출발해 동해안 지역을 여행하는 사용자
- 핵심 흐름: 여행 조건 설정 → 장소 선택 → 일정 확인
- 실행 파일: `donghae-planner.html`
- 구조: 별도 빌드가 필요 없는 단일 HTML

현재 화면은 기능 수를 줄이고 모바일 우선 탐색 구조로 다시 작성되었습니다. 지도 화면, 외부 지도 SDK, 경로 API 키 입력, 투표, 중복 추천 영역은 사용하지 않습니다.

## 유지해야 하는 규칙

1. `PLACES`는 정확히 50곳을 유지합니다.
2. `leg`는 울산에서 출발하는 장소 순서를 나타냅니다.
3. `sortCorridor()`는 `leg` 오름차순을 우선하며, 자동 추천과 직접 선택 일정에 모두 적용합니다.
4. 자동 일정의 `leg` 값은 단조 증가해야 합니다.
5. 호미곶과 대보항의 `peninsula:true` 표시는 진입과 복귀에 필요한 추가 이동시간 계산에 사용합니다.
6. 장소 정렬을 단순 위도 정렬로 바꾸지 않습니다.
7. 라이트 모드만 사용합니다.
8. 사용자 문구는 문어체 경어로 작성하고 이모지와 과장된 표현을 사용하지 않습니다.

## 화면 구조

- 상단: 제품명과 세 개의 주요 이동 링크
- 첫 화면: 기간, 출발일, 출발 시각, 여행 범위, 일정 여유, 여행 취향
- 날씨: 출발일의 울산·포항·울진 예보를 한 줄 카드로 표시
- 장소: 검색, 단일 유형 필터, 50곳 점진 표시, 네이버 지도 연결, 일정 추가
- 일정: 모바일에서는 장소 영역 앞에 배치하고, PC에서는 오른쪽 고정 패널로 비교
- 모바일 하단: 선택한 장소 수와 일정 확인 버튼
- 하단 안내: 이동시간 추정 기준과 개인정보 처리 범위

## 데이터와 연동

- 날씨는 Open-Meteo의 일별 예보를 사용합니다.
- 동일한 지역·날짜 요청은 `sessionStorage`에서 재사용합니다.
- 연결 실패 시 오류 안내와 새로고침 버튼을 표시합니다.
- 이동시간은 좌표 거리, 도로 굴곡 계수, 내륙 진입 여부를 반영한 예상값입니다.
- 화면과 일정에 실제 교통정보처럼 표현하지 않고 항상 예상값임을 표시합니다.
- 지도는 네이버 지도 검색 URL만 사용하며 장소명과 중심 좌표를 함께 전달합니다.
- 브라우저 위치 권한을 요청하지 않습니다.
- 여행 조건과 선택 장소는 `localStorage`에만 저장합니다.

## 반응형 기준

- 375px 이하: 한 열, 버튼 전체 너비, 모바일 하단 행동 버튼
- 768px: 일정이 먼저 표시되는 단일 작업 열, 장소 카드는 두 열
- 1024px 이상: 장소 탐색과 일정 패널을 나란히 표시
- 모든 구간에서 문서 전체의 가로 스크롤을 허용하지 않습니다.
- 모든 주요 버튼과 입력은 키보드 포커스 표시와 충분한 터치 높이를 유지합니다.
- `prefers-reduced-motion`을 존중합니다.

## 배포

- 환경변수와 비밀 키는 필요하지 않습니다.
- `vercel.json`은 루트 경로 연결과 기본 보안 헤더를 제공합니다.
- Web Share와 클립보드 기능을 안정적으로 사용하려면 HTTPS로 배포합니다.
- Pretendard 가변 글꼴은 CDN에서 불러오며 실패 시 시스템 글꼴로 전환됩니다.
- 자세한 내용은 `DEPLOYMENT.md`를 참고합니다.

## 검증

정적 검증은 다음 명령으로 실행합니다.

```powershell
node tests/verify-planner.cjs
```

현재 확인 항목:

- 자바스크립트 구문 정상
- 주요 HTML 태그 수 일치
- 장소 50곳과 고유 ID 50개
- 여행 범위별 `leg` 단조 증가
- 호미곶 진입 구간 표시 유지
- 375px, 768px, 1024px, 1440px에서 가로 넘침 없음
- 추천 일정 생성, 일정 복사, 모바일 하단 버튼 동작
- 네이버 지도 URL에 장소명과 좌표 포함
- 날씨 성공 상태와 연결 실패 처리 코드 존재

## 백업

- 직전 복잡한 화면: `donghae-planner.pre-simplification-20260828.html`
- 그 이전 상용화 작업 전 화면: `donghae-planner.pre-commercial-20260828.html`
- 이전 인계 문서: `donghae-planner-handoff.pre-simplification-20260828.md`

백업 파일은 참고용이며 운영 배포에는 포함하지 않는 것을 권장합니다.

---

## 추가: 동해안 드라이브 여행 플래너 (2026-08-29, 신규 다중 파일 프로젝트)

`prompt_html.md` 명세에 따라 **별도 프로젝트**를 구축했습니다. 기존 `donghae-planner.html`(동해로 단일 파일)과 `versions/` 는 그대로 보존했습니다.

- 진입점: `index.html` (+ `css/`, `js/`, `data/`, `scripts/`)
- 범위: 경주 → 고성 (부산·울산은 출발지 전용), 내장 DEMO 장소 100곳
- 핵심: 다중 날씨 종합(Open-Meteo 실호출 + 기상청/OpenWeather 어댑터) · 우천 전략 엔진(드라이브·실내 비중 자동 상향, 호우·강풍 시 해안 감점) · 회랑 기반 역주행 방지 · 네이버 지도 v3 + 목록 fallback
- 검증: `node tests/verify-planner-donghae.cjs` (92개 항목 PASS). 브라우저 QA(맑음/약한비/비/강한비/예보불일치, API 실패, 360~1920px) 완료.
- 배포: `vercel.json` 이 `/` → `index.html`, `/legacy` → `donghae-planner.html` 로 재작성. CSP 에 NAVER 지도·Open-Meteo 도메인 추가.
- 실데이터 연결·API 키 주입·보안 주의사항은 `README.md` 참조.
- `data/*.json` 은 `source:"demo"` 이며, `"live"` 로 바꾸면 앱이 병합합니다(`scripts/export-data.cjs` 로 재생성).
