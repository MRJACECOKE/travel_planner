# 동해로 디자인 시스템 규칙

이 규칙은 동해로의 모든 화면 변경과 Figma 기반 구현에 적용합니다.

> 참고: 이 저장소에는 별도 프로젝트인 **동해안 드라이브 여행 플래너**(`index.html` + `css/` + `js/` + `data/`, README.md 참조)가 함께 있습니다. 다중 파일 구조이며 명세는 `prompt_html.md`, 검증은 `tests/verify-planner-donghae.cjs` 입니다. 아래 "동해로" 규칙과 별개로 관리하고, 서로의 파일을 덮어쓰지 않습니다. 드라이브 플래너는 라이트 모드·맑은 고딕·표준 경어체를 사용하며, 우천 대응(드라이브·실내 볼거리 비중 자동 상향)이 핵심 기능입니다.

## 버전 관리

- IMPORTANT: 출시 버전은 `versions/v{major}/` 아래에 보존하고 기존 출시 파일을 직접 덮어쓰지 않습니다.
- 현재 운영 루트 파일은 `donghae-planner.html`이며 v1 호환 진입점으로 유지합니다.
- 새 주요 버전은 `versions/manifest.json`에 버전, 상태, 경로, 데이터 기준일, 변경 요약을 기록합니다.
- 각 버전은 독립적으로 열리고 검증되어야 합니다.
- 사용자 데이터 저장 키에는 주요 버전을 포함합니다.

## 구조와 구성 요소

- 이 프로젝트는 프레임워크 없는 단일 HTML 애플리케이션입니다.
- 페이지 구조는 `header`, `main`, `section`, `aside`, `footer` 시맨틱 요소를 우선합니다.
- UI 클래스는 역할을 나타내는 kebab-case를 사용합니다.
- 같은 역할의 버튼, 필터, 카드, 상태 표시는 기존 클래스를 재사용합니다.
- 기능 수보다 사용 흐름의 선명도를 우선하며 주요 흐름은 세 단계 이내로 유지합니다.

## 디자인 토큰

- IMPORTANT: 색상, 그림자, 주요 모서리 값, 글꼴은 `:root` CSS 변수로 정의합니다.
- 새 색상을 요소 선택자에 직접 하드코딩하지 않습니다. 투명도 변형은 기존 토큰을 기준으로 만듭니다.
- 간격은 4px 기본 단위의 8, 12, 16, 20, 24, 32, 40, 48, 64px 체계를 사용합니다.
- 기본 모서리는 12px, 카드 16px, 주요 컨테이너 24px을 사용합니다.
- 기본 글꼴은 `Pretendard Variable`이며 시스템 글꼴 폴백을 유지합니다.
- 본문 최소 크기는 모바일 15px, 데스크톱 16px입니다.
- 설명은 본문보다 한 단계 낮은 크기와 명도를 사용하되 WCAG AA 대비를 유지합니다.

## 시각적 위계

- 화면당 기본 행동 버튼은 하나만 강하게 표시합니다.
- 보조 행동은 흰색 또는 연한 배경 버튼으로 표시합니다.
- 카드에는 장소명, 지역, 핵심 근거, 행동 순서로만 정보를 제공합니다.
- 순위와 인기도는 출처와 기준일을 함께 표시하며 실제 방문 데이터와 대체 지표를 혼용하지 않습니다.
- 이모지, 장식 문자, 과도한 그라디언트, 자동 재생 애니메이션을 사용하지 않습니다.

## 반응형과 접근성

- 375px에서 문서 전체 가로 스크롤이 발생하지 않아야 합니다.
- 주요 터치 영역은 최소 44px 높이를 유지합니다.
- 820px 이하에서는 단일 열, 1024px 이상에서는 장소와 일정을 나란히 표시합니다.
- 모든 기능은 네이티브 버튼, 링크, 입력 요소로 제공하고 키보드 포커스를 명확히 표시합니다.
- 상태 변경 영역에는 적절한 `aria-live`를 사용합니다.
- 색상만으로 선택, 오류, 순위를 구분하지 않습니다.
- `prefers-reduced-motion`을 항상 존중합니다.

## 데이터와 추천

- IMPORTANT: 추천 순위는 장소 유형이나 수동 가중치만으로 생성하지 않습니다.
- 최근 방문 데이터가 있으면 데이터 기준일, 출처, 표본 범위를 함께 표시합니다.
- 실제 방문자 수를 제공하지 않는 데이터는 검색량, 저장 수, 길찾기 요청 등 정확한 지표명으로 표시합니다.
- 외부 데이터 연결 실패 시 과거 스냅샷을 사용할 수 있으나 기준일과 `저장된 데이터` 상태를 표시합니다.
- 데이터가 없을 때 임의 점수로 인기 순위를 만들지 않습니다.
- 비밀 키는 브라우저 코드에 포함하지 않고 서버 환경변수에서만 사용합니다.

## Figma MCP 연동 규칙

Figma 입력을 구현하는 경우 다음 순서를 생략하지 않습니다.

1. 정확한 노드에 대해 `get_design_context`를 실행합니다.
2. 응답이 크면 `get_metadata`로 구조를 확인하고 필요한 노드만 다시 조회합니다.
3. 구현 대상 변형의 `get_screenshot`을 실행합니다.
4. 구조 정보와 스크린샷을 모두 확보한 뒤 코드를 수정합니다.
5. Figma 결과를 이 프로젝트의 시맨틱 HTML, CSS 변수, 클래스 규칙으로 변환합니다.
6. Figma 스크린샷과 375px, 768px, 1024px, 1440px 결과를 비교합니다.

- Figma가 제시한 React 또는 Tailwind 코드는 디자인 표현으로만 참고합니다.
- 외부 아이콘 패키지를 추가하지 않습니다.
- Figma MCP가 제공하는 실제 에셋이 있으면 해당 에셋을 사용하고 임의 자리표시자를 만들지 않습니다.
- Figma MCP가 연결되지 않은 상태에서는 Figma 결과를 사용했다고 표시하지 않습니다.

## 검증

- 각 주요 버전은 별도 `tests/verify-planner-v{major}.cjs` 검증 파일을 둡니다.
- JavaScript 구문, 주요 태그 균형, 장소 50곳, 고유 ID, `leg` 단조 증가를 검사합니다.
- 추천 데이터의 출처, 기준일, 상태 라벨 존재 여부를 검사합니다.
- 브라우저에서 초기 상태, 데이터 성공, 데이터 없음, 연결 실패, 일정 생성 흐름을 확인합니다.
